const SUPABASE_URL=process.env.SUPABASE_URL||'https://kmbkkzhyzyyrjdphamru.supabase.co';const SUPABASE_KEY=process.env.SUPABASE_SERVICE_ROLE_KEY||process.env.SUPABASE_PUBLISHABLE_KEY;function end(res,status=200){res.statusCode=status;res.end('ok')}
module.exports=async function(req,res){if(req.method!=='POST'&&req.method!=='GET')return end(res,405);try{const id=req.query?.['data.id']||req.query?.id||req.body?.data?.id;if(!id)return end(res);const token=process.env.MP_ACCESS_TOKEN;if(!token||!SUPABASE_KEY)return end(res,500);const r=await fetch(`https://api.mercadopago.com/v1/payments/${encodeURIComponent(id)}`,{headers:{Authorization:`Bearer ${token}`}});if(!r.ok)return end(res);const payment=await r.json(),ext=payment.external_reference;if(!ext)return end(res);const headers={apikey:SUPABASE_KEY,Authorization:`Bearer ${SUPABASE_KEY}`,'Content-Type':'application/json'};if(payment.status==='approved'){const rpc=await fetch(`${SUPABASE_URL}/rest/v1/rpc/atp_process_approved_order`,{method:'POST',headers,body:JSON.stringify({p_external_reference:ext,p_payment_id:String(payment.id)})});if(!rpc.ok)console.error('RPC stock/clientes:',await rpc.text())}else{const map={pending:'esperando_pago',in_process:'esperando_pago',rejected:'pago_rechazado',cancelled:'cancelado',refunded:'reembolsado',charged_back:'contracargo'},status=map[payment.status]||'esperando_pago';await fetch(`${SUPABASE_URL}/rest/v1/atp_orders?external_reference=eq.${encodeURIComponent(ext)}`,{method:'PATCH',headers,body:JSON.stringify({payment_status:payment.status,status,mp_payment_id:String(payment.id),updated_at:new Date().toISOString()})})}return end(res)}catch(e){console.error(e);return end(res,500)}};
